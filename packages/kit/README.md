# @elfui/kit

ElfUI 的官方 Web Components 组件库，基于 `@elfui/core` 构建。

```bash
pnpm add @elfui/kit @elfui/core
```

```ts
import "@elfui/kit";
import "@elfui/kit/styles/utilities.css";
```

Labs 组件使用独立入口：

```ts
import "@elfui/kit/labs";
```

完整文档由本仓库的 `apps/website` 提供。
