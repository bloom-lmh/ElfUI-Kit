# Spacer 弹性占位

`elf-spacer` 是无 props、无事件、无插槽的语义化 Flex 占位元素，通过 `flex-grow: 1` 消耗父级剩余空间。

```html
<elf-flex align="center">
  <strong>标题</strong>
  <elf-spacer></elf-spacer>
  <elf-button>保存</elf-button>
</elf-flex>
```
