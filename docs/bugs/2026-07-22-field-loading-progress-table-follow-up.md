# 字段、Loading、Progress 与 Table 回归

- [x] Autocomplete：选中候选项后字段高度保持稳定，清空按钮固定在输入框尾部且不参与布局。
- [x] Input：动态内部前置图标可以可靠显示，图标与输入光标保留清晰间距。
- [x] Cascader：多选标签完整显示父级“研发中心”和子级标签，窄视口保持可读。
- [x] Loading：声明式与命令式全屏加载均提供明确退出按钮；全屏遮罩建立独立顶层，不能与后续局部 Loading 混层。
- [x] PopConfirm：交互结果和受控状态移到 Playground 标题状态区，案例内容只保留实际触发操作。
- [x] Progress：条形进度速度选择真实更新过渡时长；三个环形进度水平居中排列。
- [x] Table：增加带组件标题栏的完整案例，覆盖 `title` 与 `title-variant`。
- [x] 补充定向单元测试，并使用真实浏览器完成交互与截图验证。
