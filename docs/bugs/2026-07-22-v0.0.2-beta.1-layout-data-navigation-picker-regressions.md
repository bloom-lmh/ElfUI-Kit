# v0.0.2-beta.1 布局、数据、导航与选择器视觉回归修复

- 日期：2026-07-22
- 状态：已完成
- 范围：Flex、Utilities、List、BackTop、Anchor、Card、DatePicker、TimePicker

## 问题与验收

- [x] Flex 多行内容对齐案例每种只使用三个元素，仍能清楚展示 `space-between / space-around / space-evenly`。
- [x] Utilities 的 Margin、Padding、Gap 分别作用于正确的布局目标；Display 工具不再破坏设备预览内部结构或溢出容器。
- [x] List 声明式案例使用 `v-for` 渲染足量数据并出现 ElfUI 自定义滚动条；头像保持正圆且不可被压缩。
- [x] BackTop 全部案例移除原生滚动容器，统一使用 ElfUI Scrollbar。
- [x] Anchor 基础定位的平滑滚动开关行为正确；水平案例真正横向滚动，每节占满一个视口且使用更明确的深色背景。
- [x] Card 默认圆角缩小并保留主题变量覆盖能力。
- [x] DatePicker 展开面板拥有稳定的顶层层级，不被后续原生内容或 Playground 源码区覆盖。
- [x] TimePicker 页面案例在 Playground 预览区水平、垂直居中。

## 验证要求

- [x] 相关组件与页面聚焦测试通过。
- [x] 宏类型检查与生产构建通过。
- [x] 逐页完成真实浏览器验证；保存 Flex、Utilities、List、BackTop、Anchor、Card、DatePicker、TimePicker 截图。
- [x] 检查明暗主题、控制台错误和关键滚动/浮层交互。

## 执行记录

- 全量测试：`116` 个测试文件、`1029` 个测试全部通过。
- 类型检查：扫描 `106` 个宏组件文件，`0` 个宏错误、`0` 个 TypeScript 错误。
- 生产构建：Vite 成功转换 `767` 个模块并完成产物构建。
- 浏览器几何验证：Flex 三种多行对齐均为 `3` 个元素、三个独立纵向位置；List 滚动视口为 `320px`，内容高度为 `521px`；Card 默认计算圆角为 `8px`；DatePicker 打开时组件层级为 `1000`、案例容器层级为 `120`。
- 交互验证：BackTop 目标为 `elf-scrollbar` 内部滚动视口；Anchor 平滑开关可切换为 `false`，水平案例可滚动并显示持久化 ElfUI 风格横向滚动条；DatePicker 浮层不会再被后续案例覆盖；TimePicker 在预览区居中。
- 视觉验证：已覆盖 Material 与 Midnight 主题；浏览器控制台 `0` 错误。截图位于 `output/playwright/2026-07-22/`。
