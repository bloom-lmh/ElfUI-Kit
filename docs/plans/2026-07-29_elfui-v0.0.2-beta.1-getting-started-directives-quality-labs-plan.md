# ElfUI v0.0.2-beta.1 信息架构、指令、质量与实验室实施计划

- 日期：2026-07-29
- Kit 版本：`0.0.2-beta.1`
- Framework 版本：`0.1.0-beta.20`
- 状态：实施中

## 0. Vuetify 文档对照后的设计原则

2026-07-29 对照 Vuetify 的 Installation、Upgrade guide、Click outside、
Accessibility 与 Labs 页面后，ElfUI 采用其“完整学习闭环”，但不复制页面外观和内容：

1. 页面先说明用途、适用范围和稳定性，不从卡片目录开始。
2. 安装与升级使用任务路径、编号步骤、可复制代码和验证清单。
3. 指令文档同时说明适用边界、注册范围、生命周期、API 与无障碍责任。
4. 质量文档明确组件库/应用责任和“已执行/部分覆盖”的真实门禁。
5. Labs 在页面顶部提示破坏性风险，并公开提案、实验、稳定晋级条件。
6. 每页以“下一步”结束，形成可继续学习的路径。
7. 共用排版原语和主题 token，但不同领域使用不同内容模型，避免所有页面都是相同卡片。

## 1. 目标

在不复制既有 Guide、Provider、Utilities 和组件文档的前提下，建立四个职责清晰的一级目录：

1. **Getting started 快速入门**：让首次接入、升级和排障形成最短路径。
2. **Directives 指令**：为可附着到任意 DOM 元素的行为提供一致 API。
3. **Quality 质量**：集中维护可访问性、测试、性能和发布门禁。
4. **Labs 实验室**：隔离仍在验证契约的实验能力。

## 2. 信息架构

### Getting started

- Installation 安装
- Upgrade guide 升级指南
- Browser support 浏览器支持
- FAQ 常见问题

### Directives

本轮建立介绍与架构约束；子页面只有在实现、类型、测试、双语案例和浏览器验收完成后才加入菜单。

- 已有能力待统一：Draggable、Loading、Infinite Scroll
- Stable：Click Outside、Intersect、Mutate、Resize、Ripple、Scroll、Tooltip、Touch（均含独立 Playground 与 API）
- 后续统一项：Draggable、Loading、Infinite Scroll 的公共行为边界与导出

每个指令与同名组合式能力共享唯一行为内核。指令负责 DOM 绑定，组合式函数负责状态复用，不允许复制监听、定位或清理逻辑。

### Quality

- Introduction 介绍
- Accessibility 无障碍
- Testing 测试
- Performance 性能
- SSR & Hydration 服务端渲染与水合
- Compatibility & Release 兼容性与发布

本轮迁移 Accessibility 的菜单归属，保留 `/guide/accessibility` 兼容路由。

### Labs

- Introduction 介绍
- Video 视频
- Heatmap 热力图

实验组件使用独立导出面，不进入 Kit 默认注册入口。其 API 允许调整，但必须有明确实验标识和迁移说明。

## 3. 实施阶段

### P0：信息架构与入门文档

- [x] 创建四个一级目录。
- [x] 完成安装、升级指南、浏览器支持和常见问题双语页面。
- [x] 完成 Directives、Quality、Labs 双语介绍页。
- [x] 对照 Vuetify 重构内容深度：路径选择、步骤、能力矩阵、风险提示、质量门禁与下一步。
- [x] 建立共享文档排版原语：summary、section、choice、steps、flow、matrix、callout、next。
- [x] 保持四类页面各自的信息模型，不再复用单一“卡片网格”作为主体。
- [x] 接入路由、菜单图标和旧地址兼容。
- [x] 补充信息架构与中英文测试。
- [x] 完成真实浏览器与截图验收。

### P1：观察器类指令

- [x] 抽取 Click Outside 行为控制器并提供公开指令、应用注册函数、严格类型、双语案例与测试。
- [x] 实现 Intersection Observer，支持一次触发、阈值和根边界。
- [x] 实现 Mutation Observer，提供可靠卸载清理。
- [x] 实现 Resize，优先使用 `ResizeObserver` 并处理 SSR。

### P2：交互类指令

- [x] Ripple：指针与键盘触发、可关闭动画。
- [x] Scroll：目标容器、位置进度和取消清理。
- [x] Tooltip：定位、延迟和可访问名称基础设施。
- [x] Touch：方向、阈值和取消。
- [ ] 统一 Draggable、Loading、Infinite Scroll 的导出、类型与文档。

### P3：质量章节

- [ ] Testing：分层测试策略和贡献门禁。
- [ ] Performance：基准场景、预算与退化检查。
- [ ] SSR & Hydration：安全 API、延迟访问 DOM 和水合边界。
- [ ] Compatibility & Release：浏览器矩阵、版本策略和发布清单。

### P4：Labs / Video

- [x] 原生 `<video>` 状态与控制器。
- [x] 字幕、画中画、全屏、键盘和无障碍。
- [ ] 受控/非受控、错误与加载状态案例。
- [ ] 不内置 HLS/DASH；通过适配器接入第三方播放引擎。

### P5：Labs / Heatmap

- [x] 矩阵数据模型、色阶、图例与空值。
- [x] 单元格键盘浏览、焦点与语义化事件。
- [ ] Tooltip、屏幕阅读器摘要与大数据降采样。
- [ ] 大数据降采样和性能基准。
- [ ] 首版不包含地理热力图。

## 4. 完成标准

每个新增能力必须同时满足：

- 公开 API 与严格类型；
- 高内聚行为内核，指令/组合式/组件不复制实现；
- 卸载清理、SSR 安全、Shadow DOM 与 Teleport 边界验证；
- 中文、英文页面和可复制的 Template/Script；
- 单元测试、类型检查、生产构建；
- 真实浏览器交互、控制台零 warning/error 和截图；
- 计划、迁移说明、菜单和导出同步更新。

## 5. 非目标

- 不复制 Provider、Utilities、Accessibility 已有页面。
- 不以空白页面表示 Video、Heatmap 或尚未实现的指令。
- 不把实验组件加入默认稳定入口。
- 不为绕过框架缺陷而在组件层复制临时实现；确认框架问题后直接形成最小复现并反馈。
