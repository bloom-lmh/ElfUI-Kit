# 明暗主题切换 QA（2026-08-07）

执行：多 agent 并行批次 `theme_light_dark_qa`
工具：`scripts/theme-light-dark-audit.playwright.js`（可复跑，支持 `?auditStart/auditEnd` 分块）

## 结论

**146/146 路由全部通过** light（material）与 dark（midnight）切换与渲染检查：

- 强制 light / dark 各加载一次，确认 `data-theme` 正确切换、页面真实渲染（Shadow DOM 深查询 h1 + 组件树）、body 背景色实际变化；
- 真实点击顶部皮肤切换按钮（`.skin-action`），确认 skin/theme 轮换生效；
- 每页收集 pageerror / console error：**0 pageerror**。

扫描过程中因 API 清理 agent 正在保存文件，6 个 data 页面出现过瞬时 500 动态导入、多个 API 表页出现 Table `getCell` console 错误；API 清理完成后对 84–146 区间干净复测，**全部通过且 console 零错误**，确认属编辑瞬态而非主题问题。

## 静态样式审计

组件与页面样式中约 15 处硬编码白色/黑色均为有意设计（彩色填充上的对勾/圆点、媒体浮层玻璃控件、色板文字），不依赖主题变量且语义正确；**无破坏暗色模式的硬编码背景**。

## 复跑方式

```bash
node scripts/theme-light-dark-audit.playwright.js
```
