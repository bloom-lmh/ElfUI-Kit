# 死代码 / 无用文档 / 无用样式排查（2026-08-07）

执行：多 agent 并行批次 `dead_code_audit`（只读，未删除任何文件）
范围：`apps/website/src` 全量引用图、`packages/kit/src` 样式引用、`docs/` 文件名引用、页面样式类使用启发式

## A. 高置信度死代码（建议确认后删除）

| 文件                                                                                     | 证据                                                                            |
| ---------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------- |
| `apps/website/src/pages/data/VirtualListPage/ex1.ts`                                     | 完整旧示例（结构化任务列表），全仓无任何 import/测试引用；index.ts 只引 ex2/ex3 |
| `apps/website/src/pages/layout/demo-cards.scss`                                          | 全仓无 import、无 @use、无类名引用，疑似 Grid/Flex 早期 demo 样式遗留           |
| `AvatarPage/demo.scss` 的 `.avatar-demo-actions`                                         | 仅定义处 1 处，无模板/测试引用                                                  |
| `FormPage/demo.scss` 的 `.form-demo-hint`                                                | 同上                                                                            |
| `PageHeaderPage/style.scss` 的 `.page-header-api-title`                                  | 同上                                                                            |
| `StepsPage/demo.scss` / `TabsPage/demo.scss` 的 `.steps-lab-toggle` / `.tabs-lab-toggle` | 模板无该 class，仅被同文件 `:not()` 选择器自我引用（删除需连带处理）            |

## B. 不可达页面（需用户拍板）

- `layout/ContainerPage/` 与 `layout/SpacePage/`：页面、props、示例、测试完整，但路由 `/layout/container`、`/layout/space` 分别指向 GridPage/FlexPage，navItems 无入口。需确认是“被 Grid/Flex 合并取代”（保留现状）还是“路由指错”（恢复路由）。
- `labs/DocSyncPage/`：确认按用户未提交改动**从路由与导航下架**（routes/menu-icons 均无入口），组件与页面保留在仓库，本轮收口按“下架”处理。

## C. 存档类（建议保留）

- `docs/bugs/`、`docs/plans/`、`docs/reports/` 及 screenshots 归档（148 个文件名全仓无引用）——历史 QA 证据，保留。
- 组件目录内 `plan.md` / `README.md`——设计工件，保留。
- `scripts/` 下所有脚本均被 package.json / 交接文档 / 基线文档引用，无死脚本。

## D. 低置信度候选（需人工复核）

- `HomePage/style.scss` 约 11 个类（`code-card`、`principle-grid`、`starter-*`、`section-heading` 等）在当前 HomePage 模板中无引用，疑似旧版设计遗留；`APP_MESSAGES.home` 对应文案可能同步失效。
- `BackTopPage` 的 `is-active/is-done`、`draggable-demo.scss` 的 `is-drag-over/is-dragging` 等通用类名启发式未命中，误报风险高。
- `directive-demo.scss` / `layout-diagrams.scss` 各有几个类未在页面 TS 中直接出现。
- `packages/kit/src` 全部 149 个样式文件/partials 均被引用，**无死样式**。
