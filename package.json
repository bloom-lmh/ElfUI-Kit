{
  "name": "elfui-kit-workspace",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "packageManager": "pnpm@10.28.0",
  "scripts": {
    "dev": "pnpm --filter @elfui/website dev",
    "build": "pnpm build:website",
    "build:website": "pnpm --filter @elfui/website build",
    "build:lib": "pnpm --filter @elfui/kit build",
    "check": "pnpm format:check && pnpm lint && pnpm spellcheck && pnpm typecheck",
    "commit": "cz",
    "docs:locale-audit": "node scripts/audit-docs-locale.mjs",
    "format": "prettier --write .",
    "format:check": "node scripts/check-format.mjs",
    "format:check:all": "prettier --check .",
    "format:baseline:update": "node scripts/check-format.mjs --update",
    "lint": "eslint . --max-warnings=0",
    "lint:fix": "eslint . --fix",
    "spellcheck": "cspell --no-progress --show-suggestions .",
    "test": "vitest run",
    "test:kit": "vitest run packages/kit/src",
    "test:website": "vitest run apps/website/src",
    "typecheck": "pnpm typecheck:kit && pnpm typecheck:website",
    "typecheck:kit": "node scripts/check-unsupported-macros.mjs packages/kit/src && node scripts/typecheck-macros.mjs packages/kit/tsconfig.lib.json",
    "typecheck:website": "node scripts/check-unsupported-macros.mjs apps/website/src && node scripts/typecheck-macros.mjs apps/website/tsconfig.json --no-template-typecheck",
    "verify:package": "pnpm build:lib && node scripts/verify-package.mjs",
    "prepare": "husky",
    "prepublishOnly": "pnpm test:kit && pnpm build:lib"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.2.1",
    "@commitlint/config-conventional": "^21.2.0",
    "@elfui/compiler": "0.1.0-beta.20",
    "@elfui/core": "0.1.0-beta.20",
    "@elfui/router": "0.1.0-beta.10",
    "@elfui/vite-plugin": "0.1.0-beta.20",
    "@eslint/js": "^10.0.1",
    "commitizen": "^4.3.2",
    "cspell": "^9.8.0",
    "cz-conventional-changelog": "^3.3.0",
    "eslint": "^10.8.0",
    "eslint-config-prettier": "^10.1.8",
    "globals": "^17.8.0",
    "happy-dom": "^20.10.6",
    "husky": "^9.1.7",
    "lint-staged": "^17.2.0",
    "prettier": "^3.9.6",
    "sass-embedded": "^1.83.0",
    "typescript": "~5.6.3",
    "typescript-eslint": "^8.65.0",
    "vite": "^7.1.5",
    "vitest": "^4.1.10"
  },
  "config": {
    "commitizen": {
      "path": "cz-conventional-changelog"
    }
  }
}
